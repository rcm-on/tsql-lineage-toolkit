## .NET
bin/
obj/
*.user

## Output artifacts generated by the analyzer
*.graphml
*.graphify.json
graph_*.json
workflows_*.json
output/

## Demo dataset bundled with the dashboard for GitHub Pages (public sample
## data, WideWorldImporters) - deliberately tracked despite the graph_*.json
## rule above, which is meant for ad-hoc generated output, not this one.
!dashboard/demo/graph_full.json

## Local database backups (too large for git, used for manual testing)
database/

## Editor / OS
.vs/
.vscode/
*.swp
.DS_Store
